import 'package:flutter/material.dart';

class ListVille extends StatelessWidget {
  const ListVille({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Listes des maisons en local"),
      ),
    );
  }
}
